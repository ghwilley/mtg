



<!-- <div id="project-label">Auto</div>
<input id="select_origin">
<input type="hidden" id="card_multiverse_id">
<p id="project-description"></p> -->

<%= form_for(@card) do |f| %>
  <% if @card.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@card.errors.count, "error") %> prohibited this card from being saved:</h2>

      <ul>
      <% @card.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= f.label :name %><br>
    <%= f.text_field :name, :id => "select_origin" %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>